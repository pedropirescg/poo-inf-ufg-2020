package aula09.as9b.exercicio01;

import java.io.InputStream;
import java.net.ContentHandler;
/*
public class XMLReaderFactory {
    /****
     * O padrão FactoryMethod tem como objetivo isolar a criação do ojeto
     * das classes do negócio do projeto (classes de domínio do problema).
     * Para atender este padrão, esta factory precisa delegar à subclasses
     * ou outras classes sua implementação (por interface ou classe abstrata).
     *
     *
     *
     * */
/*public static XMLReader createXMLReader();
}

    public interface XMLReader {
        public void setContentHandler(ContentHandler handler):
        public void parse(InputStream is);
    }
    */